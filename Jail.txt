@name Jail
@inputs 
@outputs 
@persist Target:entity Obj:entity Angle:angle
@trigger 
runOnTick(1)
if(first()) {
    Obj = entity():isWeldedTo()
    Target = findPlayerByName("soar")
    Angle = Target:angles()
}
Obj:setPos(Target:pos() + vec(0,0,50))
Obj:setAng(Angle)

